---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "netbox Provider"
description: |-
  The NetBox provider manages resources in a NetBox https://netbox.dev instance via its REST API. Configure it with the base URL of your NetBox server and an API token.
---

# netbox Provider

The NetBox provider manages resources in a [NetBox](https://netbox.dev) instance via its REST API. Configure it with the base URL of your NetBox server and an API token.

## Example Usage

```terraform
terraform {
  required_providers {
    netbox = {
      source = "cenichub/netbox"
    }
  }
}

provider "netbox" {
  # The base URL of your NetBox server. Can also be set via NETBOX_URL.
  url = "https://netbox.example.com"

  # The NetBox API token. Prefer supplying this via the NETBOX_TOKEN
  # environment variable so it is not committed to version control.
  # token = "0123456789abcdef0123456789abcdef01234567"

  # Set to true to skip TLS certificate verification (development only).
  # insecure = false
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `insecure` (Boolean) If `true`, skip TLS certificate verification when talking to NetBox. Defaults to `false`. May also be provided via the `NETBOX_INSECURE` environment variable (`1`, `true`, `yes` enable it).
- `token` (String, Sensitive) NetBox API token used for authentication. May also be provided via the `NETBOX_TOKEN` environment variable.
- `url` (String) Base URL of the NetBox server, for example `https://netbox.example.com`. May also be provided via the `NETBOX_URL` environment variable.
